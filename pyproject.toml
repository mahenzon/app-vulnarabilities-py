[project]
name = "web-apps-vulnarabilities-py"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "csrf-attack",
]

[tool.uv.workspace]
members = [
    "hello-world",
    "csrf-attack",
]

[tool.uv.sources]
csrf-attack = { workspace = true }

[dependency-groups]
dev = [
    "black>=26.1.0",
    "pre-commit>=4.5.1",
]
